{
  "name": "f-up-counter-bot",
  "version": "1.0.0",
  "description": "A bot to count days since the last F-up",
  "main": "src/main.ts",
  "author": "Fangyi Zhou <me@fangyi.io>",
  "license": "MIT",
  "scripts": {
    "build": "tsc && esbuild out/main.js --bundle --minify --target=es2020 --platform=node --outfile=dist/main.js"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.14.49",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.101",
    "ts-node": "^10.8.2"
  }
}
